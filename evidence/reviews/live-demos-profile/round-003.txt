autoreview clean: no accepted/actionable findings reported
overall: patch is correct (0.72)
The round-002 persistence-boundary P3 is correctly closed. markSubmitting now performs a true compare-and-swap: the UPDATE is guarded by state = 'prepared' (plus NULL external_ref/result_json) and the code throws unless the run reported exactly one changed row, so a concurrent no-op loser can no longer pass by observing the winner's 'submitting' state. The new barrier test forces both runners through the initial absent reconciliation before either transitions; under either resumption order exactly one runner wins the CAS and submits, and the assertions (1 fulfilled, 1 rejected, submissions === 1, final state 'committed') hold. The effect state machine remains coherent across all traced paths: committed/observed early returns avoid adapter calls; a 'submitting' row with absent reconciliation fails closed and never resubmits; a later positive reconciliation completes through observe (which accepts 'submitting') to commit; observe's conditional guards plus post-read equality checks make conflicting cross-process observations fail closed; payload rebinding under an existing effect key is rejected; and the allow-list gate rejects non-admitted kinds before any persistence. SQLite/registry coherence checks out: the v25 live_effect_intents STRICT schema's CHECK invariants match every store transition; the four registry rows, autocommit set, and interruption-driver overrides agree; line-counting the new effect-store.ts confirms the expectation call-site lines (prepare:46, observe:68, mark-submitting:82, commit:95); user_version 25 updates, provenance entries, and docs are consistent. The rigInventory tagged mode is fail-closed at this diverged HEAD (missing tag falls back to a mismatching working-tree digest; git show failure throws), and the release test pins both the tagged digest and its divergence. Residual items considered and rejected as non-blocking within the bounded contract: 404-as-absent in the Community reconcile (wedges fail-closed in 'submitting', no duplicate effect), unbounded result_json size from adapter responses, and spawnSync buffer limits in gitBytes (fail-closed). No new P0/P1, no protected-claim-bypassing P2, and no new persistence/recovery P3.
